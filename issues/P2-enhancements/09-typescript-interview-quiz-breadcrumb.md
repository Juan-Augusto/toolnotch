# P2-09 — TypeScript Interview quiz: add breadcrumb

- **Repo ref**: `app/[locale]/interview/typescript/`
- **Source row**: 36

## Request
Add breadcrumb navigation: Home → Interview Prep → TypeScript.

## Acceptance criteria
- [ ] Breadcrumb added, consistent with `ToolWrapper` pattern
- [ ] Once done, check the other 7 interview topic pages (`database-design`, `database-indexing`, `messaging-sqs-kafka`, `nodejs-fundamentals`, `rabbitmq-concepts`, `system-architecture`, `vue` — see P3 coverage gap) for the same missing breadcrumb, since only `typescript` was in the source spreadsheet
