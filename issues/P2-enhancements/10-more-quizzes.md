# P2-10 — Quizzes section: add more quizzes

- **Repo ref**: `app/[locale]/quizzes/`, `app/[locale]/quiz/[slug]/`, `data/quizzes/`, `lib/quizRegistry.ts`
- **Source row**: 42

## Request
Expand quiz catalog (open-ended growth item, not a bug).

## Acceptance criteria
- [ ] PO to supply next batch of quiz topics/categories
- [ ] Follow existing pattern: JSON in `data/quizzes/` (+ locale subfolders if sports/trivia-type), register in `lib/quizRegistry.ts`, add to homepage + sitemap — see CLAUDE.md "Adding a new quiz" checklist
