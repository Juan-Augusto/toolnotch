# P2-03 — JPG to PDF: customizable output filename

- **Repo ref**: `app/[locale]/tools/pdf/jpg-to-pdf/` (`page.tsx` + `JpgToPdfTool.tsx`, `lib/imageToPdf.ts`)
- **Source row**: 8

## Request
Same as P2-01 (Merge PDF) — add customizable output filename input before download.

## Acceptance criteria
- [ ] Filename input field, sane default, sanitized
- [ ] Reuse the shared component built for P2-01 if one exists by the time this is picked up
