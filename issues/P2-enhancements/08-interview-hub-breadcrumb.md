# P2-08 — Interview prep hub: add breadcrumb

- **Repo ref**: `app/[locale]/interview/`
- **Source row**: 35

## Request
Add breadcrumb navigation, consistent with tool pages (`ToolWrapper` includes breadcrumb per CLAUDE.md — interview section apparently doesn't use it or lacks its own equivalent).

## Acceptance criteria
- [ ] Breadcrumb added: Home → Interview Prep
- [ ] Consistent styling with `ToolWrapper`'s breadcrumb pattern
