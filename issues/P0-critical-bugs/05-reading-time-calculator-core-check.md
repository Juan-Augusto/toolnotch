# P0-05 — Reading Time Calculator: verify core functionality + define feature scope

- **Status**: Check core functionality
- **Repo ref**: `app/[locale]/tools/text/reading-time-calculator/`
- **Source row**: 17

## Problem
Source notes "Check core functionality" and "Define better feature" — this tool's behavior/scope was never confirmed working or finalized. Needs a functional audit before it can be marked OK.

## Acceptance criteria
- [ ] Manually verify reading-time output is correct for varied text length/complexity (WPM assumption sanity check)
- [ ] Decide and document the intended feature scope (e.g. WPM selector, per-language WPM defaults, integration with word-counter)
- [ ] Confirm i18n keys exist for EN/PT/ES per CLAUDE.md convention
